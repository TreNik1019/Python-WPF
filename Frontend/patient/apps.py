"""Django application configuration for the patient app."""

from django.apps import AppConfig


class PatientConfig(AppConfig):
    """Configuration for the `patient` application."""

    name = 'patient'
