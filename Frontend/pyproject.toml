[project]
name = "frontend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.6",
    "django-environ>=0.13.0",
    "requests>=2.34.2",
]

[dependency-groups]
dev = ["ruff>=0.15.6"]
